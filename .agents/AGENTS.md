# Workspace Guidelines & Rules

## GitHub Synchronization
- After completing code modifications and verifying build/test status, always stage, commit, and push the changes to GitHub (`git add`, `git commit`, `git push origin main`).
